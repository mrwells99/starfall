# Context — where Starfall is right now

**Vertical camera and proximity fading (September11):** free orbit now reaches ±89.1°, with a terrain-swept camera sphere that allows views from under the character while staying above floors. The followed costume and weapons fade from 2.4m to 0.8m with reversible, camera-local material settings. Normal zoom preference and Outlaw shoulder aiming are preserved. All 156 focused camera/zoom/control checks and the interpolation probe passed. Native Compatibility and Forward+ captures were inspected. See the camera section in `TECHNICAL_ARCHITECTURE.md`.


## World Starwalk — September 11

World-only Starwalk is now **Ascension**: 72 jumps spiraling 59m upward around a shattered cosmic tower. Narrower landings and alternating ledges escalate into a final precision climb. Three color stages pass an eclipse, orbit frames, disintegrating ruins and star trails. The launch still requires eight continuous seconds in the cyan gate atop the eastern terrace stairs (15, 1.2, 4.5), with a private countdown; stepping out cancels it. Broad stage islands are resting spots, not checkpoints. Void falls and the gold finish return players to the center. Arena modes retain the original map; session transitions remove both collision and scenery. See `world_starwalk.gd`, `world_starwalk_scenery.gd` and the focused traversal test. Validation: 171/171 Ascension checks (all 72 full-speed capsule jumps), 57/57 World checks, and the local ENet World runner passed. Native Compatibility overview and three stage captures were inspected; rendering exited cleanly. Matching client/server builds are required.


**Class-specific bars and keybinds (2026-09-11):** customized champions now save independent ability positions and primary/secondary/general bindings. The old shared setup remains the starter for untouched classes. Selection and local actor assignment restore the appropriate profile; resets are scoped to the active class’s controls. HUD geometry stays shared. See [Edit HUD rules](GAME_DESIGN.md#edit-hud) and [implementation](TECHNICAL_ARCHITECTURE.md#class-specific-controls--september-11).

**Server disk retention:** local changes exclude development art/source libraries from Docker and avoid the duplicated project permission layer. A root-installed hook prunes obsolete Starfall image tags before pulls and after healthy deploys, preserving active/rollback/requested/feed/container-referenced images. Client downloads and volumes are untouched. Existing server needs the one-time installer after files are deployed; nothing is pushed automatically. See [deployment retention](../DEPLOYMENT.md#automatic-server-image-retention).

**Linux distribution (2026-09-10):** single static x86-64 launcher with an embedded browser Play/update panel; separate Linux feed, verified native Godot exports, Ubuntu CI job, and paired Windows/Linux/server promotion and rollback. Local launcher/race, packaging, browser and exported-game headless/rendered checks passed on EndeavourOS; publishing and Ubuntu hardware validation remain outstanding. [Player setup and release details](LINUX_DISTRIBUTION.md).

**Roster-wide network animation audit (2026-09-10):** following the Backflip fix, confirmed and corrected packet-stepped Outlaw Roll/Coin Toss, remote jump-arm overlays on all five classes, and gait/cadence oscillation caused by network position smoothing/corrections. Shared cosmetic clocks/motion inputs preserve offline poses and authoritative mechanics. New audit 96/96; existing class/pose/Backflip/hitbox suites pass. All six delayed ENet own/remote Backflip/Roll/Coin cases show zero held/reversed steps. Other regular animation/effect paths already use local clocks. [Implementation, coverage and limits](TECHNICAL_ARCHITECTURE.md#animation-timing-across-network-updates--september-10-audit). Local changes only; human online retest and publishing remain outstanding.

**Outlaw online Backflip fix (2026-09-10):** user narrowed online-only chunkiness (own and other players) to Backflip. Its special reversed-Roll seek used the 20Hz replicated timer directly. A separate cosmetic clock now advances between snapshots, preserves offline timing and follows authoritative termination. No gameplay or protocol changes. Reproduction improves from 30 held poses in 44 updates to zero; new regression 64/64, Outlaw presentation 1395/1395 and abilities 210/210 pass. Delayed ENet checks pass for own and remote views, both with zero held/reversed pose steps. Human online retest and publishing remain outstanding. [Details](OUTLAW_CLASS.md).

**Gravity Anchor visual draft r001 (2026-09-10):** Blender-authored dark core, broken rotating rings and orbiting shards now follow the existing replicated anchor state. A 0.42s appearance and gentle idle hover preserve the timer, range boundary, mechanics and character animation. Reduced effects keeps the relic static. Class identity 102/102, Fulcrum 173/173 and new presentation checks 20/20 pass. Close motion and native arena captures are in `artifacts/spell-forge/fulcrum/gravity-anchor/r001/`. Owner visual acceptance and controlled Forward+ performance validation remain open; the before/after benchmark had mismatched focus/background caps. [Spell record](../art_source/workflows/starfall-spell-forge/spells/fulcrum/gravity-anchor/RECORD.md).

**Nameplate cast row (2026-09-10):** nameplates replace their resource strip with a pooled cast-progress row beneath the unchanged 1.6×0.16 health background. Spell art sits to the left and a fitted spell name sits inside the wider cast bar. The row reads existing casting/snapshot state, follows existing interruptibility colors, and hides when idle, completed, interrupted, defeated, or on the local player. Buff/debuff icons remain above health; normal unit-frame resources are unaffected. Dedicated actors allocate no cast presentation. Validation: nameplate suite 277/277 across every castable champion ability, Team HUD 129/129; native two-caster screenshot inspected and exited cleanly.

**Ember Brand tracker:** Brand now appears as a debuff using the Flashpoint icon and an explicit ×1/×2/×3 stack count on unit frames, roster frames, and nameplates. It derives from each caster’s existing replicated `identity.brands` entries; local Ember stacks appear first and separate casters are never combined. Tooltips retain the remaining duration. Expiry, Flashpoint consumption, and pooled icon reuse clear the indicator. No spell mechanics or network fields changed. Validation: Team HUD 129/129, nameplates 68/68, native three-stack dummy screenshot inspected with clean exit.

**World targeting follow-up:** world arrival leaves selection empty. Tab/Shift+Tab select available players and stay on the paired opponent during a duel; training dummies are excluded from Tab and arena target/focus lists and remain mouse-click targets. World targeting keys run before GUI focus navigation while respecting chat, menus and edit mode. Arena frames stay hidden in World, including edit previews. The owner clarified that dummy rendering was fine, so dummy placement and visuals are unchanged. Validation: world targeting checks, team HUD checks, local world replication, and two-client network duel targeting passed.

**Current character standard: [Starfall Model Forge v2](STARFALL_MODEL_FORGE_V2.md).** The owner approved the accepted Fulcrum workflow through r008 for all future model creation, textures and animation on 2026-09-09. Use `art_source/workflows/starfall-model-forge-v2/` and the updated `$starfall-character-forge` skill. Exact preset/source/export/textures/runtime and rig/motion contracts are frozen; r006/r007 are explicitly rejected and excluded. New class identity follows its reference while mannequin anatomy and accepted movement remain uniform. V1 stays historical; other existing classes are not automatically converted. This supersedes earlier “experimental/pending workflow” statements below.

**All remaining classes converted to Model Forge v2 (2026-09-09):** Ember, Luminary and Vanguard are installed with the unchanged 53-bone UAL anatomy, original costume textures and accepted movement/casting/jump smoothing. Luminary keeps its featureless ivory hood and closed staff hand; Vanguard keeps its two-handed crystal hammer, Strike and shield presentation. Explicit grip-only motion exceptions are recorded. Class checks 972/972, jump 68/68, transitions 152/152, physical momentum 35/35, combat 81/81, Fulcrum 203/203 and rendered arena art 195/195. [Conversion, rebuild and rollback record](CLASS_MODEL_FORGE_V2_ROLLOUT.md). The frozen v2 package and Fulcrum remain unchanged; owner visual review pending.

**V1 archive storage (2026-09-09):** the owner requested lossless compression and exclusion from future Git commits. The historical v1 package now uses tar.xz plus compressed animation contracts; all 309 frozen files and the original manifest are preserved byte-for-byte. Its entire folder is ignored and removed from Git tracking. Use the current `tools/character_forge_snapshot.py --verify` or `--extract EMPTY_DIRECTORY`; copy the local folder separately for backup. V2 remains the default and its frozen package is unchanged.

**Fulcrum slight jump smoothing r008:** after reverting r006 and r007, the owner requested only a small softening of the existing head/torso motion, with modest carryover from takeoff facing/direction. Jump transitions now ease hips/spine/neck/head over **0.16s**; other joints retain the existing 0.08–0.12s timing. The displayed takeoff pose includes the current directional turn, then blends into the unchanged authored jump. No apex targets, new jump curves or shoulder-lift changes. Runtime change is limited to `fulcrum_art.gd` and `fulcrum_pose_blend.gd`; assets, arm layer and movement remain unchanged. Checks: transitions 184/184, arms/cadence 69/69, presentation 203/203. [Revision notes](FULCRUM_UNIVERSAL_ANIMATION_PASS.md#owner-feedback-revision-r008--slight-jump-transition-smoothing); reversible copies, measurements and second-screen review are in `artifacts/fulcrum-jump-smoothing-r008/`. Owner visual feedback pending.

**Fulcrum smoothing feedback r005:** clip selection remains immediate while final visible poses blend over 80–120 ms, including jump-arm layering. Interrupted blends start from the displayed pose; the weapon follows the blended hand. Actual motion/facing/combat timing are unchanged, and all prior cadence, backpedal, jump and body-turn refinements remain. New dependency `scripts/fulcrum_pose_blend.gd`; automatic second-screen review is `tools/fulcrum_transition_review.gd`. Checks: transitions 179/179, presentation 203/203, jump/cadence 69/69, physical jump momentum 35/35. [Revision notes](FULCRUM_UNIVERSAL_ANIMATION_PASS.md#owner-feedback-revision-r005--short-pose-transitions); rollback in `artifacts/fulcrum-blend-r005/`. Workflow recreation was mentioned as a later step; Forge v1 remains frozen.

**Fulcrum upper-body turn r004:** at the owner's request, all 18 left/right directional clips now turn the chest, shoulders, arms and head 20° toward side travel or 14° toward diagonal lateral travel. Pelvis heading is reduced to 65% of the leg heading and twist is shared through the spine. Foot paths and preset rest anatomy are preserved. Updated Blender/GLB installed; the earlier bent-elbow jump, faster reversed-walk backpedal and 10% running slowdown remain. Checks: Blender anatomy/source motions plus directional comparison pass; Godot presentation 203/203 and jump/cadence 69/69. Current build staging/rollback is `artifacts/fulcrum-turn-r004/`; [revision notes](FULCRUM_UNIVERSAL_ANIMATION_PASS.md#owner-feedback-revision-r004--upper-body-directional-turn).

**Fulcrum backpedal feedback r003:** backward movement and both backward diagonals now select the existing reversed walking clips at all travel speeds, rather than Run/Sprint backward clips. Normal 3.8 m/s backpedaling uses 1.15x walking cadence; actual movement speed is unchanged. Forward/sideways running retains the 10% animation slowdown and jump arms retain r002. Second-screen viewer can start with `--backpedal-review`. Checks: presentation 203/203; jump arms/cadence 69/69. See [revision notes](FULCRUM_UNIVERSAL_ANIMATION_PASS.md#owner-feedback-revision-r003--walking-backpedal); rollback copies are in `artifacts/fulcrum-backpedal-r003/`.

**Fulcrum owner feedback r002:** the owner likes the preset-based model. Jump arms now blend from the actual takeoff pose to the library's bent-elbow pose according to vertical velocity, peak at the apex, and lower on descent; the gravity weapon follows the adjusted hand. Jog/sprint cadence is 10% slower, with walking and all physical movement speeds unchanged. The dedicated second-screen jump viewer runs the live arm layer; raw Blender/GLB jump clips are unchanged. Checks: jump arms/cadence 69/69, presentation 194/194, jump momentum 35/35. Details and reversible copies: [Fulcrum pass notes](FULCRUM_UNIVERSAL_ANIMATION_PASS.md#owner-feedback-revision-r002--jump-arc-and-running-cadence).

**Fulcrum preset animation candidate (2026-09-09):** [Universal Animation pass r001](FULCRUM_UNIVERSAL_ANIMATION_PASS.md) fits the established armor, hood/mask and gravity weapon to the actual Quaternius Standard mannequin, preserving all 53 core rest transforms and retained body vertices exactly. Live Blender/GLB installed with 32 clips (11 selected source motions plus 21 derived walk/jog/sprint directions), casting and jump transitions. Remote landing uses existing snapshot ground state; simulation/capsule unchanged. Checks: presentation 194/194, native ability art 195/195, combat 81/81, jump momentum 35/35. Viewer has all clips and targets the second display; local rollback copies exist. No uncontended performance benchmark while the owner plays another game. Owner feedback pending; this experimental Fulcrum override takes precedence over the old rig/gait instructions for this class only. Frozen Forge v1 remains unchanged.

**Living spell workflow:** [Starfall Spell Forge](../art_source/workflows/starfall-spell-forge/README.md), draft 0.1, stores the spell-production process, performance safeguards, per-spell template and owner-feedback ledger. Use it for future spell animations and revise it from the owner's feedback, preserving accepted traits and recording whether a preference applies to one spell, a class or all spells. No spell visuals are approved yet and no generic effect framework was implemented during setup. It complements the Character Forge; the character archive remains frozen.

**Reusable character workflow:** the owner requested a named, comprehensive preservation of the exact character process. Use [`STARFALL_CHARACTER_FORGE_V1.md`](STARFALL_CHARACTER_FORGE_V1.md), “Starfall Character Forge v1,” or `$starfall-character-forge`. Its immutable-by-convention archive under `art_source/workflows/starfall-character-forge-v1/` includes all four current characters, recipes, references, textures, animation contracts and verification context. Verify its hashes before reuse; preserve later project edits instead of restoring it wholesale. New class appearances may vary with their references; the process and animation baselines are fixed and inspectable.

_Short-term memory. Keep concise. Historical decisions live in [`DECISIONS.md`](DECISIONS.md); long-range plan lives in [`ROADMAP.md`](ROADMAP.md)._

Last updated: **2026-09-09**.

**HUD pass 1:** personal/target/focus unit frames now render cached resource meters for Heat/Resolve/Meditation and three Star cells, with personal spending/empowerment threshold legends and Fulcrum instant-proc text. Gold TARGET and violet FOCUS headers explicitly label ally/enemy; existing health borders retain team colors. Resource controls are children of the existing status slot, preserving frame/auras indices and saved frame positions. HUD editing shows sample resources. Validation: 21 resource checks and 216 UI checks passed, including frame input and saved HUD editing; all four actual champion loadouts were captured in a staged native 3v3 at 2560×1440, and the review exited 0. Follow-up completed: stable team/enemy rows show spell casts, 1.2-second interruption feedback, prioritized control countdowns, defensive states and low-HP text; enemy rows expose arena slots 1–3. Combat text combines rapid numbers into separate damage/healing lanes, protects important events, expires centrally and caps visible labels at 36. Shortened headers keep the enemy column inside the viewport. Cast-bar border overlays now default transparent, keeping cast text readable. HUD edit previews retain valid sample rows. Team HUD 33/33, resource 21/21, spectator 22/22 and UI 216/216 passed; layout captures cover 1280×720, 1920×1080 and 3440×1440. Native staged 3v3 uses authored champions at 2560×1440. Short RX 7800 XT Balanced sample: mean frame 16.801ms, p95 16.684ms, max 81.389ms; GPU mean 5.160ms. Other review work overlapped the sample, so this is a smoke check, not a controlled before/after benchmark or proof the Windows hitch is resolved.

**Connection rejection visibility:** `leave_session(reason)` now presents a persistent highlighted connection notice within the main menu instead of sending the reason to the obscured combat notice. It includes the full server-provided version mismatch text and Dismiss. Starting another connection clears it; dismissal clears the stored status. Explicit text width prevents the wrapped label from expanding the menu off-screen on initial layout.

**Arena target/focus controls:** Keybinds now includes Target arena 1–3 and Focus arena 1–3, with primary/secondary assignments and existing save/conflict behavior. New actions default to unbound to preserve user bindings. They resolve through the same enemy_ids order as the enemy rows, including defeated slots; missing rows do nothing. Right-clicking an enemy row sets focus without changing target or capturing the camera. Existing Set focus (F) and Target focus (G) remain.

**World training and ability library:** three stationary training dummies spawn only in world mode, using reserved actor IDs -100/-101/-102 and existing snapshots. Server damage clamps HP at 1 while still reporting full damage; dummies never fight, cannot be displaced or challenged to duels, and are absent from match arenas. Their positions are (-6, 0.1, -13), (0, 0.1, -13), (6, 0.1, -13). Character selection no longer has a Practice button or hover instruction; All Abilities opens a scrollable full-kit screen beside the rotatable model. Back/Escape return to the originating Online/Offline submenu, retaining selection. Validation: world 20/20 (including headless-server direct damage and DoTs), combat 81/81, introduction 46/46, UI 200/200, and 33 rendered menu layout cases passed. Real world admission/fixture replication and two-peer social/duel/disconnect suites passed. Native all-abilities and world-dummies captures inspected; render review exited 0. Local changes only, not deployed.

**Champion introduction (item 7):** Online/Offline now pair the tall rotatable portrait with role/playstyle, resource advice, three signature icons and immediate illustrated hover cards containing full live-kit descriptions. The oversized native hover and click dialog were removed; the custom card wraps to a compact 360-unit text width and includes a 48-unit icon. The former practice action was replaced by the All Abilities browser; training now uses world dummies. `champion_introduction.gd` owns guide presentation; nested connection menus retain just the portrait. Validation: 38 introduction checks and 200 UI checks passed after the hover-card change; the earlier 30 layout cases passed. Native hover screenshot inspected and capture exited 0. Native 1440p/1080p captures inspected and two consecutive runs exited 0; earlier screenshot teardown stalls did not reproduce, so do not claim a root-cause fix. The capture tool now logs before scene teardown and reports completion after it. No audio, gameplay balance or network changes.

**Eliminated-player experience (item 4):** `spectator_presentation.gd` provides the spectator card, remaining teammate count, buttons/target-cycle keys, automatic survivor switching and an eight-second recent incoming-event recap. The camera follows the selected teammate’s interpolated pose; dead-player action bars are hidden. Results, rematches, menus and world duels are handled without changing authority or RPCs. Damage events lack spell names, so recap reports attacker/damage plus existing event labels. Validation: 22 spectator checks (including key cycling and screen bounds), 200 UI checks, 30 polish checks; native staged spectator captures inspected. Added the fast spectator suite to CI. Native screenshot reviews reached their completion marker and produced inspected captures, but both final runs timed out during teardown (exit 124); native clean shutdown remains unverified. Headless and rendered UI suites exited 0. Local only; no deployment.

**Champion selection follow-up:** the main menu no longer shows the champion dropdown. Online/Offline (including queue/host/join) show a model portrait above the dropdown, with all other mode/action controls below. The chosen champion is shared across those screens and still drives the existing match/join actions. `champion_preview.gd` loads one existing model at a time in an isolated viewport, poses its idle clip once, and redraws only for selection/visibility/size changes. It is disabled outside selection and in headless/UI-fixture rendering; no continuous animation, shadow pass, or audio. The portrait is now a taller centered 3:4 frame, with a larger model, 2× supersampling (1536px cap), 4× MSAA and linear filtering. Drag left/right to rotate; release stops redraws. Its height adapts to available menu space. Native captures include all four champions and offline at 2560×1440; full silhouettes were visually checked. Layout checks passed all 30 cases at 720p/1280×800/1080p; UI checks passed 200/200 and polish checks 30/30.

**Visual repass of polish items 1/3/5:** owner requested a substantially more pleasing presentation after the functional pass. `menu_presentation.gd` now styles launch with larger play cards/typography, centered content-sized panels and a contained status area; settings actions follow their fields. Results have a distinct outcome heading, winning-team caption, three statistic cards and emphasized rematch action. Ability restrictions use an inset colored outline/strip plus a separate colored tooltip explanation. All additions use ordinary 2D controls/drawing; no new 3D effects or audio. Validation: actual CI UI wrapper 200/200, polish wrapper 30/30, real dedicated rematch/waiting clients, and 30 layout cases (10 states × 720p/1280×800/1080p) passed. Native Forward+ captures reviewed at 1920×1080 and 2560×1440 (final saved set) under `artifacts/polish-*.png`. No push/deploy performed. The buddy is investigating whole-frame Windows/NVIDIA stalls locally; average GPU ~30% does not identify the cause. Do not pursue that investigation instead of the authorized visual work.

CI UI duration follow-up: the owner observed a roughly six-minute silent UI step that eventually advanced to the map suite. Xvfb uses llvmpipe CPU rendering; the UI suite unnecessarily drew the full arena and fighters. It now instantiates `tests/ui_test_arena.gd`, retaining production UI/input/camera/actors and arena collision while omitting environment art and disabling viewport 3D drawing. Map, ability-art and Vanguard rendered suites remain unchanged. Added startup/progress messages, explicit scene teardown, and a three-minute UI step timeout. Validation: the exact Xvfb Compatibility wrapper command passed all 200 checks and exited 0 on local llvmpipe. Hosted Ubuntu timing is still unmeasured; this does not accelerate a workflow already running the previous revision.

CI dependency-install follow-up: the runner's unrelated Google Chrome repository returned package metadata with a hash mismatch, aborting `apt-get update` before rendered tests. The test job now pins Ubuntu 24.04 and scopes both update/install to `/etc/apt/sources.list.d/ubuntu.sources`, ignoring third-party source fragments. Ubuntu fetch failures remain fatal and signature/checksum verification stays enabled; transient downloads get three retries. This failure was runner dependency setup, separate from the earlier test-summary and disconnect regressions. Validation: workflow YAML and shell syntax passed; the exact install step completed with exit 0 in a disposable Ubuntu 24.04 container containing an intentionally broken third-party repository. All UI dependencies installed without accessing that repository. The hosted workflow itself has not been rerun here.

CI disconnect follow-up: `peer_disconnected` now connects with `CONNECT_DEFERRED`. Godot 4.5.1 ENet emits the server disconnect signal before erasing the transport peer; immediate lobby broadcasts could therefore send into its already-closed channels, especially when all five clients left results together. Defer departure processing until polling finishes, also protecting world departure RPCs. Validation: six-peer, normal network/rematch/bot takeover, dedicated rematch/departure/waiting, private lobby, world entry, and social world despawn runners all exit 0 without engine errors.

CI follow-up: corrected `arena_lod_test.gd`'s summary to the shared wrapper's `N passed / M total` format. The assertions already passed, but `N/M passed` was rejected by `check_suite.sh`. Verified through the actual wrapper: 248 passed / 248 total, exit 0. Future CI suites must be validated through their workflow command, not only by invoking Godot directly.

**Further GPU optimization (same pending 0.11.0 batch):** the owner reports smooth play at 60 FPS/100% scale and ~75% → ~30% GPU use after the earlier changes. Added offline arena LODs (48 levels; packed full-detail geometry/UV2 preserved) and skipped zero-contribution floor shader noise. With the owner’s editor/game closed, four native 1080p six-player samples measured GPU mean 3.696 ms before versus 3.565/3.567 ms after (~3.5%); fixed overview LOD-only saving ~7%, close-up negligible. No promised utilization target or low-end hardware guarantee. Position-only shadow mesh experiment was rejected for visual regression. See `FORWARD_PLUS_ASSESSMENT.md` for method and limits. Validation: mesh integrity 248/248, map 57/57, combat 81/81, plus native close/gameplay/overview image review. No audio changes.

Latest polish/performance work (**0.11.0**, 2026-09-09): the owner authorized checklist items 1/3/5, then prioritized a friend's reported RTX 5060 Ti lag. Added explicit client frame budgets (60 gameplay by default, 30 menus/results, 15 unfocused), persisted 60–240 FPS choices, Balanced/High/Performance graphics, 3D resolution scaling, and Show FPS. Balanced is now the default; it preserves models, textures, geometry, baked lighting, palette, bloom, SSAO and 2× MSAA while disabling SSIL, volumetric fog, and shrine point-light shadows. High restores those optional effects. Existing aura styles are reused while tint stays unchanged. **The friend's resolution/FPS and a hardware retest are still pending; no cause or speedup is claimed.** Another editor/game was using the local GPU, so no uncontended benchmark was attempted.

The same local batch completes menu-state cleanup (including an overview camera and Quit), shared read-only ability-availability checks/badges/tooltips, offline Play again, replicated automatic rematch countdown/waiting status, and a duration/survivor summary. Returning roster broadcasts no longer interrupt clients still in an active round/results. `finish_round` now carries metadata, so client/server must both restart on 0.11.0. Validation: combat 81/81, class identity 91/91, polish 30/30, rendered UI 200/200; delayed team networking and dedicated countdown → rematch → opponent-leaves → waiting flow passed. Native Forward+ preset transitions and four captures were verified at actual 1920×1080. No deployment/push performed. **Audio is owned by the user's sound-engineer friend; exclude audio from agent work.** See `POLISH_CHECKLIST.md` and `TECHNICAL_ARCHITECTURE.md` for the current implementation.


Latest map CI fixture fix (2026-09-09): terrace tests changed the bot's champion name to Vanguard but retained Ember's ranged kit. Range-based bot navigation correctly stayed at casting distance, causing six failures. The fixture now installs Vanguard's kit, resets cooldowns/identity, and retains the real walking/collision assertions with attacks disabled. Xvfb CI suites explicitly select Compatibility rendering and Dummy audio to avoid unsupported Vulkan/audio initialization on hosted runners. Production renderer and bot behavior are unchanged. Validation: map 57/57 headless and 57/57 through the exact Xvfb CI command.

Latest CI parse fix (2026-09-09): the merged remote network test referenced six undeclared jump-observation variables. Restored their persistent state declarations and added a direct parser check for `tests/network_peer.gd` plus delayed movement coverage to CI. The local branch predates those merged jump assertions; validation uses an isolated checkout of remote revision `321914a` at `/tmp/starfall-ci-jump-fix`, with the same small patch applied. Validation passed on that merged revision: parser, duel, team, latency, movement with latency, and jump momentum 35/35. The current branch was not fast-forwarded; no remote push or deployment.

Latest CI network-test fix (2026-09-09): team replication test could fail when autonomous bots interrupted the single Stoke attempt before the hard seven-second rematch. Added a test-only arena fixture holding bot decisions through the scripted first round, restoring them for rematch/disconnect. Stoke retries until acknowledged; rematch waits for attack evidence with a twelve-second deadline. All replication assertions remain; PASS output is now conditional and the Python runner requires both success markers. Production gameplay unchanged. Validation passed: duel, team (twice), latency, movement with latency, and six-peer 3v3. An isolated negative control dropping both combat actions failed the expected assertions and printed no PASS markers.

Latest local 3v3 performance fix (2026-09-09): owner also reproduced severe lag offline, so dedicated-server measurements alone did not address their problem. Removed per-tick border StyleBox allocation, composed complete overhead nameplate text once, and cached impact/death material state across all four authored classes. Map quality, meshes, animation rate, physics and combat stay unchanged. A rendered local six-fighter Forward+ comparison on RX 7800 XT at 1280×800 / 60 FPS target improved mean frame interval 25.68→16.67 ms (~39→60 FPS), p95 77.48→16.72 ms. Short seeded runs with full HP; not a promise for the owner's hardware. `tools/local_match_benchmark.gd` repeats the rendered team workload (`-- --bench-duel` for two fighters). Validation: combat 81/81, class identity 90/90, rendered UI 198/198, rendered ability art 193/193. The final benchmark averaged 16.86 ms with p95 16.69 ms, though an isolated 96.9 ms frame remained during a run with other validation work active. User still needs to retry local 3v3; no deployment performed.

Latest server optimization (2026-09-09): dedicated actors now retain collision and combat state without loading authored models. Dedicated mode skips HUD/animation/local VFX updates and player settings; its frame loop is capped at the unchanged 60 Hz physics rate (snapshots remain 20 Hz). The headless map already skipped artwork. Compose now starts only duel, team, world; four private lobbies require the `private-lobbies` profile. Local seeded six-bot CPU dropped 92.8%→7.8% of one core and sampled RSS 169.3→130.3 MiB; these are local comparisons, not cloud capacity estimates. Combat 81/81, focused server runtime 27/27, dedicated queue/late join, private lobby, world chat/duel/despawn, and delayed-network movement/combat/rematch passed. Repeatable Linux benchmark: `python tools/measure_server.py`. See DEPLOYMENT sizing for methodology. No deployment performed; production resource readings are still needed after deploying. RPC surface and version remain unchanged.

Latest gameplay work: **0.10.0** preserves world-space takeoff velocity through jumps (including input release, reverse and turning), with authoritative grounded snapshots for prediction. Positive spell targeting ranges are scaled to 75%, rounded to 0.5m and capped at 18m, below the 20m opposing spawn distance. Inward grants one stored instant, off-GCD Collapse; its own 18s cooldown still applies. Graviton now lasts 11s, deals 3 per second per stack, stacks twice per caster and generates no Meditation. The owner's “two charges” means two target DoT stacks, not stored casts. New **Entropy** (default Shift+7) is instant, lasts 15s, ticks for 2 damage and 5 Meditation, and refreshes one stack per caster. Fulcrum Starfall damages enemies within 5m of its selected target with a matching cast warning. Tutorial text is removed and chat occupies a shorter 160px bottom-left panel. Existing saved keybindings are preserved; a conflicting new default leaves Entropy clickable and unbound. Full current numbers: `CLASS_ABILITIES.md`; implementation and validation: `TECHNICAL_ARCHITECTURE.md`. Character models and the archived forge workflow are unchanged. Matching client/server builds must be restarted; no deployment performed.

Connection gate retained from **0.9.1**: negotiates version and RPC schema before allowing any scene RPC, preventing old snapshots from being dispatched to `ping_host`. Regression cases cover version mismatch, same-version RPC mismatch and legacy servers.

Previous class work (**0.9.0**, superseded where noted above) introduced Fulcrum Meditation, empowered Collapse at 75+, an instant Graviton proc on Collapse hits, and Starfall (Shift+6). Starfall requires 50+ and spends all Meditation for scaling damage. Inward/Outward/Collapse/Heavy Orbit ignore LOS while retaining range and collision constraints. Completed DPS Mend cleanses attached DoTs. Mend heals its full 28 (capped by missing HP) even in long matches; other healing is only dampened in arena matches, never in the world. The instant Graviton proc remains stored until used and flashes its action-bar icon.

Previous gameplay work: **0.8.0** adds world-only duel buttons and replicated invitations, session chat (Enter; rebindable), world disconnect despawning, and a draggable CC edit preview. Late arrivals no longer rebuild existing characters/cameras. Combatants interpolate between physics ticks and the camera follows the same rendered position; instantaneous simulation inputs remain. Network changes require matching client/server builds; no deployment performed. Implementation and tests: `TECHNICAL_ARCHITECTURE.md`, “World social UI and presentation timing”.

Latest environment work: the owner approved the corner treatment across the entire map, with the original glowing artwork restored on all four pillars. All 27 authored modules now share the upgraded materials; the covering plaque was removed, opposing wall shrines added, and Forward+ High lighting made the default on restart. Existing physics and baked-light assignments are preserved. `--sanctum-base` disables High effects; `--rendering-method gl_compatibility` selects Compatibility. The latest palette pass slightly reduces bronze warmth/shine and removes random purple emission from stone and floating debris while retaining the intentional runes. High remains the default. The comparison viewer remains available. Current workflow: `ART_SLICE_HANDOFF.md`; prior renderer study and its concurrent-host timing caveat: `FORWARD_PLUS_ASSESSMENT.md`.

Next character handoff: the owner requested this workflow be logged before asking for another class. Read [`CHARACTER_PIPELINE.md`](CHARACTER_PIPELINE.md) first: it preserves the references, exact files, local tool paths, rig/clip settings, build commands, verified results, implementation lessons and outstanding quality issues.

Latest Fulcrum work (2026-09-08, approved weapon integration): the owner loved the model/colors/textures and requested the floating gravity weapon from the reference, first as a separate review or with rollback available. After viewing the study, the owner explicitly authorized integration. Live `art_source/fulcrum.blend` and `assets/characters/fulcrum.glb` now include the black singularity orb, counterrotating broken rails, violet light and suspended debris above a cupped left hand. Body geometry, material settings and packed texture images match the approved original; UV rebuild rounding is below 0.00013 texel. Current totals: 83,958 vertices / 157,160 triangles / 62 bones / two meshes / 17 surfaces, 12 unique materials. Seven clips remain, with only the carrying arm and new weapon controls intentionally adapted. Godot passed Fulcrum 1,068/1,068, combat 81/81 and rendered art 179/179 (1,328 checks). Blender verified weights, loops and 392 core gait samples within 1.79e-7 of Ember. The full builder now requires `fulcrum_details.py` and `fulcrum_weapon.py`. Original source, asset and related files are backed up under `artifacts/fulcrum_before_weapon/`; complete build/restore notes are in `CHARACTER_PIPELINE.md`. No gameplay changes or remote push.

Latest Luminary work (2026-09-08, hood revision): the owner explicitly requested an Ember-like hood in Luminary's style, with no facial features or protruding hair. The exposed face, scalp, ears, neck, hair geometry and two hair controls have been removed, not hidden. `art_source/luminary.blend` and `assets/characters/luminary.glb` now contain an ivory/gold lined hood, featureless recessed dark veil, cowl and halo. The seven clips, staff grip and cape motion remain. Current source: 79,301 vertices / 150,476 triangles / 59 bones / 13 surfaces. `scripts/luminary_art.gd` dispatches from `champion_model.gd` alongside Ember and Vanguard. Post-revision verification: Luminary 865/865, combat 81/81, rendered art 91/91; Blender verified skin weights, no hair controls/facial materials, stance height and 392 core-gait samples identical to Ember. See the current hood revision in `CHARACTER_PIPELINE.md`; its older open-face section is historical.

Latest Ember work (2026-09-08): the owner rejected the faceted model and rigid movement and requested a Blender-authored character based on the supplied hooded celestial mage reference. `art_source/ember.blend` and `assets/characters/ember.glb` now provide a smooth skinned model with 52 bones and seven clips, integrated through `scripts/ember_art.gd`. The old Ember construction branch is removed. `scenes/ember_preview.tscn` is an interactive animation viewer. See the Ember section in `ART_DIRECTION.md` for workflow and remaining visual limitations.

Latest Vanguard (2026-09-08, new reference): the owner requested the same Blender process using a heavy crystal-armored knight concept. The live model has a sealed T-visor crystal crown, forged armor, inset-crystal hammer, navy embroidered tabard/cape, 45 bones and the original eight clips. Six new cape controls add motion; 12,168 original bone/frame samples match within floating-point tolerance. Source 123,044 triangles. Vanguard 799/799, combat 81/81, rendered art 179/179. Current source, Windows commands, limitations and backup: [`VANGUARD_REFERENCE_REBUILD.md`](VANGUARD_REFERENCE_REBUILD.md). This new appearance awaits owner review. Other champions and gameplay were preserved. The 90% usage stop rule remains; older readings are historical. Arena handoff: `ART_SLICE_HANDOFF.md`.

## Class identity implementation — 2026-09-08

Owner authorized the four class concepts with baseline mobility, Mend/sustain, and damage reduction retained. Each class now has twelve abilities on 1–7 / Shift+1–5; every ability has an icon. Full current rules: [`CLASS_ABILITIES.md`](CLASS_ABILITIES.md). `class_mechanics.gd` implements resources, stars, ally interception, gravity fields and exchanges; `Combatant.identity` is replicated. Protocol version is 0.6.0, requiring matching client/server builds. No deployment was performed. Human balance testing remains next. Validation: class mechanics 89/89, baseline combat 81/81, world 13/13, rendered art 131/131, UI 192/192; real ENet team/latency test (including second-bar input and resource snapshots) and six-peer match passed. New art totals about 1.2MiB; eight generated icons plus reused existing art cover all 48 slots.

## Current milestone

**Playable prototype with dedicated-server mode landed.** The game runs in two modes: **Offline** (local sparring vs bots) and **Online** (join a dedicated server or a player-hosted lobby). The pipeline that would actually deploy that server to DigitalOcean does not exist yet.

## Objective

Close the "push to `main` → buddies play the new build" loop:

1. GitHub Actions builds Linux/Windows/Mac exports on push to `main` and publishes a GitHub Release tagged with `Config.VERSION`.
2. A DigitalOcean droplet runs the Linux server binary under `systemd`; Actions SSH-deploys and restarts the service.
3. A small client-side launcher (bash + `.bat`) checks GitHub Releases on startup, downloads the new build when `Config.VERSION` differs, and launches Godot.

Until all three exist, buddies play by manually launching Godot from a checkout.

## Recently completed (this batch)

- **Instant movement and full keybinds menu (2026-09-08):** movement inputs now register the same physics tick they arrive on. `Input.use_accumulated_input = false`, the 30 Hz client input gate is gone, and `scripts/movement_prediction.gd` predicts local motion each tick and reconciles against snapshots (`move_ack`, `velocity`, `motion_revision`). Non-movement state stays server-authoritative. A new **Settings → Keybinds** menu (`scripts/keybind_menu.gd` + `scripts/key_bindings.gd`) rebinds movement, targeting, duel keys and all 21 action-bar slots with primary + secondary bindings, conflict-swap, search, per-row clear and reset-all; Esc/F11/Alt+Enter are reserved. Ember and Vanguard locomotion clips blend in ~0 s so animation matches the new input responsiveness. `Config.VERSION` bumped to **0.7.0** — snapshot payload grew, mismatched clients are rejected. Coverage: `tests/movement_bindings_test.gd` 23/23, `run_network.py --test-movement --test-latency` verifies start/reverse/strafe/stop respond under 150 ms simulated latency, combat 81/81, screenshots at `artifacts/keybind-menu.png` and `artifacts/keybind-bars.png` via `tools/keybind_review.gd`.

- **Sanctum lighting and landscape:** shared live/baked lighting profile, lower ambient/fill, warm shrine lighting and a new indirect bake; large exterior cliffs, hanging foundations, broken approaches, distant sanctuaries and subtle non-volumetric mist. Landscape6batches/19,116triangles, no colliders/shadow passes. Map57/57, combat81/81, world13/13. Assets remain just under8MiB. Current complete handoff: `ART_SLICE_HANDOFF.md`.

- **Authored Sanctum arena:** expanded the approved section to all four covers, the full floor, all boundary walls and both terraces/ramps, using shared Blender-authored meshes, UV-based PBR materials and native baked lighting in Compatibility. No new collision. Runtime environment budget approximately7.94MiB including meshes and lightmap; map57/57, combat81/81. Rebuild and continuation notes: `ART_SLICE_HANDOFF.md`. Actual render: `artifacts/sanctum-slice.png`. Cosmic backdrop, portals and atmospheric decor retain the previous procedural pass.

- **Cosmic Sanctum richness pass:** distinct basalt, worn floor, blue terrace tile and bronze materials; triplanar derivative relief; true emissive glyphs; animated banners, embers and floating debris; two distant watcher shrines; depth fog and a cached sky with a cheap motion layer. Visible cover now matches its existing collision width. No new textures or colliders. Map 57/57 and combat 65/65; visual review in `artifacts/sanctum-*.png`. Details in `ART_DIRECTION.md`.

- **Roster art pass:** 20 painted ability icons cover all three seven-slot kits; original in-engine Ember, Vanguard and Luminary models replace capsules, with procedural movement/cast/hit/defeat poses. Owner explicitly authorized this art work. See `ART_DIRECTION.md`; review boards regenerate with `tools/art_review.gd`.

- **Theme established — Cosmic Gladiators.** Space + fantasy: summoned fighters from different worlds, an ancient arena floating in space, celestial temples, nebulae, gods watching; deep purples/blues with extremely bright magical accents. Set by the user, now canon in [`ART_DIRECTION.md`](ART_DIRECTION.md) and [`DECISIONS.md`](DECISIONS.md). The owner subsequently authorized ability icons and player models; see the roster art pass above.
- **Documentation reorganized** into `docs/` as persistent shared memory for AI agents. Main README rewritten as a short operating manual.
- **Dedicated server mode** — `--dedicated --mode=team --min-players=2 --rematch-delay=8`. No local player, auto-start when threshold met, auto-rematch after each round, holds in lobby when roster drops below threshold.
- **Version handshake** in `register_player` — hard reject on mismatch.
- **Buffs and debuffs** — `scripts/auras.gd` derives every effect from the timers the simulation already keeps, shown as hoverable chips on unit frames and on overhead nameplates.
- **Edit HUD** — drag frames, rebind hotbar keys, swap ability positions. Client-side only; bar positions translate to kit indices before reaching the simulation.
- **Display settings** — window mode and resolution, persisted to `user://starfall.cfg` with the HUD layout and keybinds.
- **Fulcrum initial implementation (historical, version 0.3.0):** a fourth champion with mid-range control and `Tether`. Its original icon/model fallbacks have since been replaced, and its current twelve-slot gravity kit is documented in `CLASS_ABILITIES.md`. Current Blender model work is recorded above.
- **Cosmic sanctum map** (parallel agent session) — new arena geometry, layout, sky shaders and environment texture, with `tests/map_test.gd`, 57 checks. Bot pathing and line of sight verified intact against it.
- **Queue fix** — a dedicated server now accepts players who connect mid-round and holds them for the next one. Previously the queue only worked in the idle seconds between matches; private lobbies were unaffected, which is why they worked while the queue looked broken. `Config.VERSION` is now **0.2.0** (the `lobby_state` RPC gained a field), so every client must be re-downloaded.
- **Ability icons and champion models** (parallel agent session) — generated icons for all twenty abilities, wired into the hotbar behind the cooldown overlay, plus built champion models replacing the primitive placeholder bodies. Carries `tests/ability_art_test.gd`, 84 checks.
- **Icon assets downscaled 1254px -> 256px before the first push**, 44 MB to 2.4 MB. They display at 92 px; git keeps blobs forever. See the asset budget in [`ART_DIRECTION.md`](ART_DIRECTION.md).
- **HUD pass** — WoW-style radial cooldown sweeps with OmniCC-style countdowns on the hotbar, single-hover ability tooltips (the Shift-expanded variant is gone), and a cast bar under the overhead nameplate. No art needed for any of it; the overlay works unchanged once icons land.
- **Online menu rewritten** — Online now offers **Online queue**, **Host lobby**, and **Join lobby**. No server address or port appears anywhere in the UI.
- **Online queue** — picks the port from the selected mode (duel 27840 / team 27841), shows "Searching for an opponent…" with an `n of m players ready` count, and drops into the round when the server auto-starts.
- **Private lobbies** — a fixed pool of `--dedicated --lobby` processes on 27850–27853. Host lobby claims the first idle slot and returns a 4-character code; Join lobby probes the pool for that code. No broker process. Reasoning and rejected alternatives in [`DECISIONS.md`](DECISIONS.md).
- **Server is now six containers** from one image — `docker-compose.yml`, deployed by GitHub Actions to GHCR and then over SSH. See [`../DEPLOYMENT.md`](../DEPLOYMENT.md).
- New `scripts/config.gd` centralizes deploy-time constants. `SERVER_ADDRESS` points at `play.leafmods.com`.
- **Docker deployment** — `Dockerfile` (pinned Godot, pre-built import cache, non-root, read-only rootfs), `docker-compose.yml`, `.env.example`, `.github/workflows/deploy.yml`, `deploy/bootstrap.sh` + `install-docker-rocky.sh` + `create-deploy-user.sh`, and [`../DEPLOYMENT.md`](../DEPLOYMENT.md). The old systemd model is archived in `deploy/legacy-systemd/`.
- New integration tests `tests/run_dedicated.py` (queue) and `tests/run_lobby.py` (private lobbies, including probing past a claimed slot).

## Known-good state

Roster art validation: combat 47/47, UI 73/73, art/model checks 84/84 (all three classes on both teams). Review boards: `artifacts/champion-lineup.png`, `champion-lineup-back.png`, `ability-atlas.png`. The UI suite requires a virtual screen large enough to reach the hotbar; see `ART_DIRECTION.md`.

All six suites pass as of this batch: `combat_test` 47/47, `ui_test` 64/64, `run_network`, `run_six`, `run_dedicated`, `run_lobby` all exit 0.

Watch for `ERROR:` in test output — `run_network.py`, `run_six.py`, `run_dedicated.py` and `run_lobby.py` all fail the run if the string appears. A `Control` created but never added to the scene tree leaks its font and canvas RIDs at exit and trips exactly this check; that is how the orphaned `address` field was caught.

## Repository hygiene

`artifacts/` is **no longer tracked**. It holds screenshots and review renders that every test run rewrites, so tracking them added multi-megabyte blobs to history for output that is regenerated on demand — `.git` reached 371 MB against a ~90 MB working tree. `artifacts/.gdignore` is kept so Godot does not import whatever lands there, and docs still reference the paths, since the review tools recreate them locally.

Untracking stops the growth but does not shrink existing history. Reclaiming that 371 MB would need a history rewrite (`git filter-repo`), which is disruptive with a shared remote and has deliberately not been done.

`tests/check_references.py` fails CI when a committed file references a resource that is not itself committed. Two agents in one repository will otherwise ship half a coupled change: a preload of a file, or a call to a method, whose other half is still local. It catches the file case; the method case is only caught by actually running the suites, so **verify the staged tree rather than the working tree** — `git archive $(git write-tree)` into a temp directory and run them there.

## Known blockers

None. **The pipeline is live**: push to `main` runs the suites, builds the image, pushes to GHCR and deploys six containers to the droplet, pinned to `sha-<commit>`. `play.leafmods.com` resolves to it.

Nobody has played a match against the droplet yet — the health gate only proves the ENet socket is bound, not that a client can reach it from outside. That is the next thing to verify.

Three traps hit during the first live deploys, all fixed, all worth knowing:

- A leftover `ringfall.service` from the pre-Docker model held UDP 27840, so `starfall-duel` could not bind. Never run both deployment models on one droplet.
- The GHCR package is created **private** on first push; the droplet cannot pull until it is made public or given a `read:packages` login.
- **Test scripts do not import assets.** A fresh checkout has no `.godot/imported/`, and `godot --script` will not build it — every `.ctex` fails to open and the Python harnesses fail on the `ERROR:` lines even though all assertions pass. CI now runs `--import` before the suites. This was invisible until the ability icons arrived, because the project previously had no assets at all.
- **A "healthy" container can be unreachable.** The container healthcheck runs inside the network namespace, where the server always binds its port; it cannot see a failed host-side publish. `starfall-duel` reported healthy for an hour with nothing listening on 27840, because the old systemd service held that port when the container was first created. `starfall-deploy` now fails the deploy if any container has no published ports. Symptom to recognise: `ss -ulnp | grep 278` shows fewer than six listeners while `docker ps` shows six healthy.
- `docker/metadata-action` sets `outputs.version` from the **highest-priority** tag, and `type=raw` outranks `type=sha` by default — so deploys pinned the mutable `latest` until explicit priorities were set. Waiting on droplet provisioning (user is spinning it up) before running the first manual deploy. Nothing in the lobby/queue work has been exercised against a real droplet yet — only against local processes.

## Immediate next steps (in order)

1. **Droplet provisioning** — user creates the Rocky 9 droplet, adds `A` record for `play.leafmods.com`, runs `deploy/bootstrap.sh`, adds the four GitHub secrets, pushes to `main`. Runbook in [`../DEPLOYMENT.md`](../DEPLOYMENT.md).
2. **Verify buddies can connect** — Online queue into a match, and a shared code into a private lobby, from their own machines.
3. **First real CI run** — the workflow is written and locally validated but has never executed against a droplet. Secrets and host-key pinning are the likely first failures.
4. **Client-side launcher** with forced-update-on-mismatch behavior.
5. Once the loop closes, real playtesting. Feel-tuning follows.

## Queued next

**Client distribution is the open gap.** The server auto-deploys on every merge to `main`; buddies do not. They run from a checkout or a ZIP of the repo, so after each push someone has to tell them to re-download. `Config.VERSION` is still `0.1.0` and nothing bumps it, so a stale client is accepted and can desync instead of being cleanly rejected. Closing this is roadmap item 3: CI exports Windows/Linux builds to GitHub Releases plus a small self-updating launcher.

## Explicitly not in progress

- Extracting subsystems from `scripts/arena.gd` (~3070 lines).
- Audio and a full skeletal animation pipeline.
- Authentication, host migration, anti-cheat, ranked/skill-based matchmaking.

## Movement and keybinds — work in progress

Immediate local movement prediction and shared collision movement are implemented in `movement_prediction.gd` / `arena.gd`, with authoritative reconciliation, movement acknowledgments and displacement revisions. Version 0.7.0. Settings → Keybinds adds search, primary/secondary bindings, movement/targeting/bar rows, persistence and conflict swapping shared with Edit HUD. Locomotion start/stop/direction animation blending is removed. Baseline combat 81/81, movement/keybind checks 23/23, existing UI 192/192, team/latency network passed before final polish. A new 150ms movement-direction test initially used world axes for a reversed-facing client; corrected to local axes, but its rerun was blocked by automatic approval review reporting workspace credits exhausted. Final rendered header/search polish and that network retest remain pending. Do not claim final movement validation complete.

## Unique icons — 2026-09-08

Seventeen generated replacements remove every icon collision between distinct active abilities. 45 distinct abilities / 48 slots; only the same Mend ability shares art across classes. File/content audit passes, all new PNGs are 256px, both full and 55px contact sheets inspected. `UNIQUE_PROMPTS.md` records prompts and output files. Godot import/render remains pending under the approval-credit block; no deployment performed.

DR completion validation: 76 category/lifecycle/UI checks, combat 81/81, class identity 91/91, Fulcrum 111/111, team HUD 36/36 and UI 216/216 passed. Six-peer ENet verified independent category replication; handshake tests passed for version/schema/protocol/legacy rejection. Simple icon placement was reviewed in native 3v3 and at 720p. Current DR columns face inward: enemy left, party right; active buffs/debuffs remain below health. No deployment performed.

Resource-label removal: unit-frame resource meters are now text-free, including captions, numeric totals and ability/proc hints. The visual meter/Star cells and threshold ticks remain in a compact 16px slot, also used by Edit HUD previews. Party/arena thin strips are unchanged.

Resource thickness adjustment: party/arena strips are now 6px tall (previously 3px); overhead resource quads are 0.07 world units tall (previously 0.035). Effect rows move down 3px to preserve spacing. Personal/target/focus resource meters are unchanged.

Roster heading removal: party and enemy arena columns now start directly with their health bars; the PARTY/ARENA headings are removed.

Health label simplification: all player/target/focus/party/enemy health bars now show only a percentage (fixed 100 maximum HP), including Edit HUD previews. Class, role, arena slot and keybind text are removed from health bars; colors and bindings remain.

Typography rollback: restored the earlier health-bar font size and 1px shadow, 15px hotbar key labels and 22px match heading. Typeface was unchanged; this restores the earlier text treatment while retaining percentage-only health labels and minimal resource/nameplate layout.


### Combat frame hierarchy (September 9)
Self and target now anchor above the action bars (260px health width, 32px height); focus is a smaller 180px frame with a gold edge. Party rows hide the local player but retain their original indexes, so party keybinds and row clicks keep their meaning. In arena 1v1 the enemy overview is hidden and enemy DR appears to the left of the target; self DR appears to the right of the personal frame. Team/world enemy lists retain their DR columns and selected enemies receive a stronger edge. Explicitly moved HUD positions survive; saved untouched defaults adopt the new layout. Percent-only health, text-free resources, floating auras, and the restored font remain.

The default self/target pair uses mirrored center-bottom offsets (-370/+110 for 260px bars), keeping the combined group centered at any viewport width. A 220px center gap fits both DR groups; compact focus sits outside the pair at +410. Centering and bounds are checked at six resolutions, including 2536×1427 and ultrawide.

Self/target health widths reduced 15% to 221px with mirrored -331/+110 offsets and unchanged 220px DR gap; focus follows at +371. Party/enemy rows now fit visible content (40px quiet, up to 91px with a cast and aura icons), include both DR rows when needed, and use a 2px inter-row gap.

Roster layout supersedes the dynamic-height experiment: party/enemy rows are fixed at 70px (2px separation), with a 52px health bar, up to six 22px effect icons inset at upper left, a dedicated upper-right health percentage, and a 17px inset cast strip. Resources remain 6px below health; DR remains outside, with space for both rows. Casts/effects appearing or expiring never change row height or position.

Roster health bars slimmed from 52px to 40px; inset effects are 20px and casts 15px. Fixed row positions and external DR space are preserved.

Party/enemy resource and cast positions swapped: a 10px resource strip is inset at the bottom of health, with the 15px cast below health. Rows stay fixed at 64px with 1px separation. Roster-only DR tiles are 28px with 2px gaps so both rows fit; primary-frame DR tiles remain unchanged.

Default self/target gap tightened from 220px to 152px while keeping 221px widths and a centered pair (-297/+76 offsets). Focus follows at +337. Primary-frame DR uses two columns per group to fit all six categories without overlap; roster DR is unchanged.

World-nameplate buff/debuff icons enlarged 2.5× (0.28 to 0.70 world units), with 0.76 spacing and 0.48 vertical offset above health. Visible icon groups center above the nameplate. HUD/DR icon sizes are unchanged.

Removed the separate personal aura display and its Edit HUD entry. Personal buffs/debuffs and hover descriptions remain on the player unit frame; old SelfAuras saved positions are ignored.

All class resource bars now share mana blue (#327ce6), across self/target/focus, party/arena, nameplates, and Edit HUD previews. Class health colors and resource thresholds/segments are unchanged.

Team readability: class health colors remain intact. Other fighters show a red triangle (enemy) or cyan circle (ally) beside nameplates, with matching unshaded ground rings. The selected non-local actor has white nameplate brackets and a thicker white selection ring; the local actor never receives either ground ring. Party/arena rows carry matching symbols on their outer edge, opposite DR. Marker meshes are created once, have no shadows, and use viewer-relative team checks; dedicated actors do not instantiate them.

Removed team triangle/circle symbols from world nameplates. Team ground rings, selected-target white brackets/ring, and party/arena row symbols remain.

Owner priority update: spell VFX and client distribution/performance-balance work deferred; friend owns movement/animation and reportedly fixed his machine’s freezing. Next big run backlog: comfort settings, practice tools with bot difficulty on Offline menu, and friend-joining/recovery improvements. Combat feedback is currently a design discussion, not yet authorized for implementation. See POLISH_CHECKLIST.md current priorities.

Follow-up preferences: Classic-style floating combat text for combat outcomes, building on existing aggregation; Easy/Normal/Hard bot presets; reclaim a bot-controlled character on same-match reconnect if reasonably simple. Rejoin feasibility and secure session identity still require investigation.


## Quality-of-life batch completed locally
- Edit HUD now shows party/enemy placeholders even in a live duel; drag input is consumed and party positions persist. Real input regression test added.
- `player_options.gd`: persisted mouse sensitivity/invert, UI/text scale (90–125%), reduced combat flashes/effects, Offline Easy/Normal/Hard AI behavior and passive opponents. Separate local reset controls restore health/resources/effects or cooldowns, blocked online. Reduced effects retain gameplay boundaries and status indicators.
- `combat_text.gd`: repeated outcome messages merge; local outgoing/incoming (spectator-follow when applicable) floating text preserves numeric aggregation and prioritizes interruptions/immunity/dispels/CC breaks. Other actors still get frame interruption feedback. Mitigation is labeled Reduced, not Absorbed, because shields are currently reductions.
- `session_recovery.gd`: connection progress, retry/copy-code, and current-client-session reconnect tickets. Disconnect reserves existing actor for 90 seconds in same active round/server; bot controls actor until valid ticket reclaims it. Health/cooldowns/DR remain, input sequence counters reset, ticket rotates. No client/server restart recovery, world rejoin, or host migration. Compatible game/server deployment required; not deployed here.
- Menus use a bounded scroll container at short heights or larger UI scale. Comfort has its own modal panel.
- Validation: UI 220/220 including live party drag and saved reload; QoL 21/21; combat 81/81; spectator 22/22; DR 76/76; team HUD 108/108. Real ENet host/client and private lobby suites passed; new reconnect suite rejects forged ticket and verifies same actor/HP/CD/DR after new-peer reclamation. Native Forward+ menu/combat render review passed; menu captures at 100%/125% on 1280×720 passed bounds checks. New unit/reconnect tests wired into CI.

Owner requested removal of manual reset health/resources and cooldown controls. Both buttons, their container, and the reset handler are deleted. Existing automatic duel/round resets, difficulty presets, and passive opponents remain.
