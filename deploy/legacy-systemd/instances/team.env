# 3v3 queue. min-players is the number of HUMANS needed to start; the remaining
# slots are filled with bots, so 2 starts a round as soon as a pair is waiting.
STARFALL_ARGS=--dedicated --mode=team --port=27841 --min-players=2 --rematch-delay=8
